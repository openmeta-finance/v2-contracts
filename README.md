# Openmeta V2

This repository contains the core smart contracts for the Openmeta Trade Protocol. 

Try running some of the following tasks:

```shell
npx hardhat help
npx hardhat test
REPORT_GAS=true npx hardhat test
npx hardhat node
npx hardhat run scripts/deploy.js
```
